export function setMeals(state, meals) {
  state.meals = meals;
}

export function setLetterMeals(state, letterMeals) {
  state.letterMeals = letterMeals;
}

export function setLoading(state, val) {
  state.isLoading = val;
}
